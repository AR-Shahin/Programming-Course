#include <iostream>
using namespace std;
#define PI 3.1416
int main()
{
    float r, c;
    float area;
    cout << "Enter Radius : ";
    cin >> r;
    area = PI * (r * r);
    cout << "Area of Circle " << area;

    float f = (c * (9 / 2.0) + 32);
    return 0;
}
